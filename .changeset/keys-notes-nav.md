---
"hunkdiff": minor
---

Rebind inline notes to `i` and all notes to `a`, list every note in the review grouped by file, dismiss menus on an outside click, scroll the viewport with `ctrl+e` and `ctrl+y`, and put the navigation commands in the Navigate menu.
